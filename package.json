{
  "name": "practice-village",
  "private": true,
  "scripts": {
    "build": "esbuild member-auth.js --bundle --format=esm --outfile=assets/member-auth.bundle.js"
  },
  "dependencies": {
    "@netlify/blobs": "^10.7.13",
    "@netlify/identity": "latest",
    "pdf-lib": "^1.17.1",
    "stripe": "^22.5.0"
  },
  "devDependencies": {
    "esbuild": "latest"
  }
}
