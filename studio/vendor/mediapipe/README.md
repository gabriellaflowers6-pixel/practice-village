# MediaPipe Tasks Vision runtime

These files are vendored from `@mediapipe/tasks-vision` version 0.10.12 so the
practice page can run without loading scripts or models from a CDN.

The Pose Landmarker model is the `pose_landmarker_full` task distributed by
Google's MediaPipe project. See `LICENSE` for the upstream Apache 2.0 license.
